// NOTIFICATIONS TEMPORARILY DISABLED
// Tests skipped until notifications are re-enabled

describe.skip('useNotifications', () => {
  it('should skip all notification tests', () => {
    expect(true).toBe(true);
  });
});
