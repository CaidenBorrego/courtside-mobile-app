// NOTIFICATIONS TEMPORARILY DISABLED
// Tests skipped until notifications are re-enabled

describe.skip('NotificationService', () => {
  it('should skip all notification service tests', () => {
    expect(true).toBe(true);
  });
});
